Python后端依赖：
- Django 4.2.11 (核心Web框架)
- python-dotenv 1.0.1 (环境变量管理)
- qrcode (二维码生成，从views.py代码中使用但未在requirements.txt中列出)
- Pillow/PIL (图像处理，从views.py中使用)
- SQLite3 (数据库，Django内置支持)

前端依赖：
- Tailwind CSS (通过CDN引入，用于UI样式)
- Font Awesome或类似图标库 (通过SVG图标使用，用于UI装饰)
- 原生JavaScript (无额外前端框架依赖，使用原生DOM操作)