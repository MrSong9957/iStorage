1. 修改`base_template.html`文件中的导航栏部分
2. 为导航栏添加一个容器，使用与页面主体内容相同的`max-w-4xl`最大宽度
3. 保持logo在左侧，登录按钮在右侧的布局
4. 确保导航栏内容居中显示，与页面主体内容对齐
5. 保持页面风格的延续性，符合极简主义设计原则
6. 只修改必要的代码，不影响其他功能

修改后的导航栏代码结构：

```html
<nav class="fixed top-0 w-full h-16 bg-white/80 backdrop-blur-md border-b border-gray-200 z-50">
    <div class="w-full max-w-4xl mx-auto flex justify-between items-center px-6 h-full">
        <div class="flex items-center gap-2">
            <div class="w-6 h-6 bg-black rounded-md"></div> <span class="font-semibold text-gray-900 text-lg">iStorage</span>
        </div>
        <div>
            <a href="{% url 'users:user_login' %}" class="text-gray-900 hover:text-gray-600 text-sm font-medium">登录</a>
        </div>
    </div>
</nav>
```

